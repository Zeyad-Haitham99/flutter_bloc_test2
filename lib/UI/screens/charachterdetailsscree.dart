import 'package:flutter/material.dart';

class CharachterDetailsScreen extends StatelessWidget {
  const CharachterDetailsScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}